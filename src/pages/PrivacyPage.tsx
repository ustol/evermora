import { PagePlaceholder } from "@/components/layout/PagePlaceholder"

export default function PrivacyPage() {
  return (
    <PagePlaceholder
      title="Privacy policy"
      description="Placeholder — requires legal review before production launch."
    />
  )
}
